import datetime
import credentials

embedcolor = 0x0092E2

bot_name = "Big Ben"
footer = f"© {datetime.datetime.now().year} - {bot_name}"
prefix = "$"

host = credentials.host
user = credentials.user
passwd = credentials.passwd
database = credentials.database
token = credentials.token
